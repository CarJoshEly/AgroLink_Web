# URL base de la API (interna en NestJS o una API externa).
# Mientras esté vacía, la app usa datos mock (lib/mock/) automáticamente.
NEXT_PUBLIC_API_URL=

# Cuando conectes Supabase Storage para imágenes de producto / DNI / selfies:
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
